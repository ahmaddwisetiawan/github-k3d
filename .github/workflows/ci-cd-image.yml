name: CI/CD K3D

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: "${{ secrets.DOCKER_USER }}/nginx-k3d"

jobs:
  build-push-image:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Build Image
        run: docker build -t $IMAGE_NAME .

      - name: Login to Docker Registry
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      - name: Push Image
        run: docker push $IMAGE_NAME

  deploy-k3d:
    needs: build-push-image
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Install kubectl
        run: |
          if ! command -v kubectl &> /dev/null; then
            curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x kubectl
            sudo mv kubectl /usr/local/bin/
          fi

      - name: Setup Kubeconfig
        run: |
          mkdir -p $HOME/.kube
          # Mengambil config cluster langsung dan simpan ke folder user student
          sudo k3d kubeconfig get k3s-default > $HOME/.kube/config
          sudo chown $USER:$USER $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Replace variables
        run: envsubst < webserver.yaml.template > webserver.yaml

      - name: Deploy to K3D
        run: |
          # Paksa kubectl menggunakan config yang baru dibuat
          export KUBECONFIG=$HOME/.kube/config
          kubectl apply -f webserver.yaml
          kubectl apply -f webserver-svc.yaml

      - name: Verify
        run: |
          export KUBECONFIG=$HOME/.kube/config
          kubectl get pods
          kubectl get svc

name: CI/CD K3D

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: "${{ secrets.DOCKER_USER }}/nginx-k3d"
  # Menambahkan lokasi kubeconfig agar kubectl bisa akses cluster
  KUBECONFIG: /home/student/.kube/config

jobs:
  build-push-image:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Build Image
        run: docker build -t $IMAGE_NAME .

      - name: Login to Docker Registry
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      - name: Push Image
        run: docker push $IMAGE_NAME

  deploy-k3d:
    needs: build-push-image
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Install kubectl
        run: |
          if ! command -v kubectl &> /dev/null; then
            curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x kubectl
            sudo mv kubectl /usr/local/bin/
          fi

      - name: Replace variables and output to webserver.yaml
        run: envsubst < webserver.yaml.template > webserver.yaml

      - name: Create deployment
        run: kubectl apply -f webserver.yaml

      - name: Create service
        run: kubectl apply -f webserver-svc.yaml

      - name: View status
        run: |
          kubectl get deployments
          kubectl get services
